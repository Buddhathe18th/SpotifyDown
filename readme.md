# SpotifyDown

A simple Python application to download Spotify songs

---

### Setup

Make sure you have:
- Python 3.8 or newer  
- `pip` (Python package manager)  

Try and run 
````
venv\Scripts\activate
pip install -r requirements.txt
python app.py
````